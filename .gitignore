# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NEVER commit secrets (WHOOP client secret, Anthropic key, Supabase service role)
.env
.env*.local
.env.local
.env.development
.env.production

# typescript / test
*.tsbuildinfo
/coverage

# editor
.vscode/*
!.vscode/extensions.json
.idea

# private / medical data — never commit real PHI
/private/*
!/private/.gitkeep

# Netlify build artifacts (generated locally by the CLI/build)
.netlify/
deno.lock
