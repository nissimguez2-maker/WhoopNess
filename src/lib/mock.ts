// Intentionally empty. Mock data was removed — the app uses live WHOOP data now.
// Kept as an empty module because Netlify's incremental deploy was retaining the
// old (deleted) file from a prior snapshot; an empty module overwrites it cleanly.
export {};
